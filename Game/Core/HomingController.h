#pragma once

#include "Core/Core.h"
#include "Volt/Game/Entity.h"

class Entity;
/* Controls an entity's velocity to hit the target entity. */
/*
class HomingController {
public:
    HomingController (Entity* entity, Entity* target, float speed,
                      float turningSpeed)
        : m_target(target),
          m_entity(entity),
          m_speed(speed),
          m_turningSpeed(turningSpeed) {
    }

    void Update ();

private:
    Entity* m_entity;
    Entity* m_target;
    float m_speed;
    float m_turningSpeed;
};
*/
